#LSF文件 的初步规律

基于 `adv.zip` 样本可确认：

- 文件头为 `LSF\0`
- 头部含版本与记录数
- 每条记录大小约 164 字节
- 每条记录前 128 字节是 PNG 文件名
- 随后的 9 个 32 位整数中，前 4 个可与 PNG 尺寸精确对应：
  - `left`
  - `top`
  - `right`
  - `bottom`
- `tag` 字段的低字节和次低字节表现出明显的“分组码 / 变体号”规律

程序里的 LSF 模式，就是按这个规律来做合成的。
